import 'package:flutter/material.dart';

class RateAppPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Notez l\'application')),
      body: Center(child: Text('Merci de noter l\'application !')), 
    );
  }
} 